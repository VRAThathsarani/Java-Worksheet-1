import javax.swing.*;

public class Four {
    public static void main(String[] args) {
        JFrame myFrame;
        myFrame = new JFrame();
        myFrame.setSize(300, 200);
        myFrame.setTitle("My First Frame");
        myFrame.setLocation(100, 50);
        myFrame.setVisible(true);
    }
}

// myFrame.setLocation(0, 0) positions the window at the top-left corner of the screen.